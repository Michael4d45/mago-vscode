{
  "name": "mago-vscode-server",
  "description": "Mago language server",
  "version": "0.1.0",
  "engines": {
    "node": ">=16.0.0"
  },
  "main": "./out/server.js",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "prepublish": "npm run compile"
  },
  "dependencies": {
    "vscode-languageserver": "^8.1.0",
    "vscode-languageserver-textdocument": "^1.0.8",
    "@iarna/toml": "^2.2.5"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
